{
  "name": "@agent-tasker/operator-console",
  "version": "0.0.0",
  "private": true,
  "description": "Static operator console for submitting tasks and inspecting coordinator results.",
  "type": "module",
  "scripts": {
    "build": "node scripts/verify-static.mjs",
    "typecheck": "node scripts/verify-static.mjs",
    "test": "node scripts/verify-static.mjs",
    "clean": "rm -rf dist"
  }
}
